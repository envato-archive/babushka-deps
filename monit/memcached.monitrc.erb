# <%= generated_by_babushka %>

check process memcache_<%= var(:memcached_port) %>
  with pidfile /var/run/memcached.pid
  start program = "/etc/init.d/memcached restart"
  stop program = "/etc/init.d/memcached stop"
  group memcached
